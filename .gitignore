node_modules
config.jsonc
data/audit.json
keys/

# Superseded by config.jsonc, but still present on existing deployments
# until their secrets are migrated. Never commit these.
.env
config.json
admin.json
env.js

# IDE stuff
.vscode/*